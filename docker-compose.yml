version: "3"

services:
  webapp:
    container_name: cst-webapp-prod
    image: cst-webapp-prod
    build: ./webapp
    environment:
      - API_HOST=api
    depends_on:
      - api
    ports:
      - "443:80"

  api:
    container_name: cst-api-prod
    image: cst-api-prod
    build: ./api
